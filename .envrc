export WDP=${PWD}
export PATH="${PWD}/bin:${PATH}"
export PATH="${PWD}/.tools:${PATH}"

export GO111MODULE=on

export POSTGRES_PASSWORD="postgres"
export TEST_CACHE_URL_REDIS="redis://localhost:7100/0"
export TEST_DATABASE_URL_POSTGRES="postgres://postgres:${POSTGRES_PASSWORD}@localhost:7200/postgres?sslmode=disable"
